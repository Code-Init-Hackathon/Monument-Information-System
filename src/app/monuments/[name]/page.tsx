import Card from '../../card'

export default function MonumentDetail(){
    return (
        <div className="max-w-4xl mx-auto p-6">
          <h1>Hello</h1>
          {/* <div className="bg-white shadow-lg rounded-lg overflow-hidden">
            <img className="w-full h-64 object-cover" src={monument.imageUrl} alt={monument.name} />
            <div className="p-6">
              <h2 className="text-3xl font-bold text-gray-800">{monument.name}</h2>
              <p className="mt-4 text-gray-600">{monument.description}</p>
              <div className="mt-6">
                <h3 className="text-xl font-semibold text-gray-800">Coordinates</h3>
                <p className="text-gray-600">Latitude: {monument.coordinates.lat}</p>
                <p className="text-gray-600">Longitude: {monument.coordinates.lng}</p>
              </div>
            </div>
          </div> */}
        </div>
    );
}